Create side cars for image files, with links back to source/embedded doc, descriptions of images, etc

[Done]
Add a header to all docs in raw that has common frontmatter
- Date created
- last date indexed
- last date updated

Add to the wiki pages that every things has a date associated with it in the wiki links. this will enable us to check the dates and ensure the information is fresh, and if a new source refutes the information then the wiki pages can be updated with the new data, remove the old source links, but keep the original documents for reference.  

