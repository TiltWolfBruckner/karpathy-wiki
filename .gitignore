# Obsidian per-vault state (auto-created on open; varies per machine)
.obsidian/
.obsidian.vimrc

# OS / editor artifacts
.DS_Store
*.swp
*~

# Local Claude Code overrides (machine-specific)
.claude/settings.local.json

# Notion ingestor's pointer to the user's index page (workspace-specific)
notion-index.md
